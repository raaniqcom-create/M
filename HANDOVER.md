# تسليم — المحطة التقنية

ما تحتاجه جلسة جديدة لتكمل بلا إعادة اكتشاف. حدّثه كلما تغيّر شيء جوهري.

## البنية

- **الموقع**: Next.js بتصدير ساكن (`output: 'export'`) على GitHub Pages، الدومين `muhta.online`.
  لا خادم — كل منطق الخادم في دوال Supabase Edge.
- **قاعدة البيانات**: Supabase، المشروع `snlafcvuoxpxcdbtinsy`.
- **التطبيقان**: غلافا Capacitor WebView يحمّلان `muhta.online` مباشرة.
  **كل تغيير في الويب يصلهما فور إعادة الفتح.** البناء يلزم فقط لما هو داخل الحزمة:
  الأيقونة، شاشة البدء، `alert.caf`، الصلاحيات، `Info.plist`.
- **البوت**: `@muhtaonlinebot` — دالة `telegram`، بلا حالة إلا جدول `telegram_drafts`.

## الدوال المنشورة

| الدالة | ماذا تفعل |
|---|---|
| `telegram` | البوت كاملاً: تسجيل محطة، إدارة، مفضّلة |
| `notify` | تنبيه توفر: تيليجرام + FCM لأندرويد + **APNs مباشرة لـ iOS** |
| `otp` | رموز SMS عبر OTPIQ: تسجيل، استعادة كلمة مرور، اشتراك مواطن |
| `admin-alert` | شكوى/طلب تسجيل → أجهزة الإدارة + تيليجرام |
| `broadcast` | إرسال عرض للمشتركين (إدارة فقط، خطوتان) |
| `station-phone` | نقل محطة إلى رقم صاحبها الحقيقي (إدارة فقط) |

## قرارات لا تُنقض بلا سبب

- **iOS يخاطب APNs مباشرة، لا عبر Firebase.** تمريره خلال FCM يعني إضافة حزمة Firebase
  إلى Xcode و`GoogleService-Info.plist` في البناء ومورّداً ثانياً — مقابل قفزة لا تشترطها Apple.
- **الازدحام للمسرب لا للمحطة.** لون واحد للساحة يصرف سائقاً عن مضخة خالية.
  المسارب الثلاثة: `gasoline_premium`, `gasoline_regular`, `kerosene`.
- **سؤال الازدحام بعد الرحلة فقط.** التصويت على بطاقة التصفح يطلب حكماً ممّن لم يذهب —
  أنتج صوتين في يومين. حُذف.
- **لا حذف تلقائي عند تشابه المحطات.** يُعرض التشابه، والقرار للمدير بعد مكالمة.
- **الرقم هو رقم النشر واسم الدخول معاً** (`p<digits>@muhta.app`). تغييره ينقل الملكية،
  ولذلك يمرّ بـ `station-phone` لا بتحديث حقل.
- **التصدير ساكن**: أي مسار ديناميكي يحتاج `generateStaticParams`. لذلك صفحة محطة الإدارة
  هي `/admin/station/?id=` لا `[id]`.
- **حقول الإدخال ١٦ بكسل على الأقل**: iOS يكبّر الصفحة عند التركيز على أصغر منها ولا يعيدها.

## أسرار — أين تعيش

لا شيء منها في المستودع (عام). أسرار Supabase تُضبط بـ `supabase secrets set`،
وأسرار GitHub بـ `scripts/set-gh-secret.py`.

`OTPIQ_API_KEY` · `APNS_KEY_ID` · `APNS_TEAM_ID` · `APNS_PRIVATE_KEY` · `APNS_TOPIC` ·
`FIREBASE_SERVICE_ACCOUNT` · `TELEGRAM_BOT_TOKEN` · `TELEGRAM_ADMIN_IDS` · `PRE_LAUNCH`

GitHub: `APPSTORE_KEY_ID` · `APPSTORE_ISSUER_ID` · `APPSTORE_PRIVATE_KEY_B64` · `APPLE_TEAM_ID`
· `ANDROID_KEYSTORE_B64` · `KEYSTORE_PASSWORD` · `GOOGLE_SERVICES_JSON_B64`

**Apple Team ID**: `4MY5C2893K` · **App Store Connect app id**: `6799531591`
**Play developer**: `9098331717988618244` · **Play app**: `4975984216221300866`

## حالة النشر

- **Google Play**: `1.5.0 (7)` في المراجعة منذ ٩ أغسطس ٢٠٢٦.
- **App Store**: `1.5.0 (8)` **Waiting for Review** منذ ١٠ أغسطس ٢٠٢٦.
  حساب المراجعة `07000000001` / `MuhtaReview2026`، التصنيف `4+`، Navigation،
  مجاني في ١٧٥ دولة، والنشر تلقائي بعد الموافقة.

## ما أصلحه فحص ما قبل المراجعة

قبل الإرسال بساعة، فحص متعدد الوكلاء وجد أربعة عيوب حقيقية — ثلاثة منها كانت
ستُرفض حتماً:

- **iOS لم يكن يسجّل في APNs إطلاقاً.** `AppDelegate.swift` كان قالب Capacitor
  الافتراضي بلا `didRegisterForRemoteNotificationsWithDeviceToken`، فيُهمَل الرمز
  ولا يُكتب أي صف في `device_tokens`. المفتاح والصلاحية والنغمة كانت صحيحة كلها،
  والحلقة الأولى مفقودة — أي أن الإشعارات لم تعمل يوماً على آيفون.
- **لا حذف حساب داخل التطبيق** (5.1.1(v)) وسياسة الخصوصية توثّق المخالفة بنفسها.
  الحل: دالة `delete-account` وزر في لوحة المحطة.
- **الحزمة كانت universal** بلا لقطات iPad ولا تصميم لها → صارت للآيفون فقط.
- **`status` بلا حماية في RLS**: أي صاحب حساب كان يستطيع نشر محطته فوراً بـ
  `status:'approved'`. الحل: مُشغِّل `stations_guard_trg`.

## ما تبقّى

1. **DSA**: التوزيع في الاتحاد الأوروبي يتطلب توثيق حساب التاجر من صفحة
   App Information، وإلا حُجب التطبيق في أوروبا وحدها.
2. **منشور «انضم إلى المحطة التقنية»** لصفحات التواصل.
3. **قياس الازدحام بعد شهر** — إن بقي التفاعل صفراً رغم إصلاح التوقيت، تُحذف الميزة.
   مؤشر فارغ دائماً يعلّم المستخدم أن جزءاً من التطبيق لا يعمل.

## الأهم، ويسبق كل ما سبق

**ست محطات حقيقية فقط في محافظة كاملة.** هذا أضعف ما في المنصة، أضعف من أي ميزة ناقصة —
ويراه مراجع المتجر والسائق معاً. التسجيل من البوت يأخذ دقيقة للمحطة.
