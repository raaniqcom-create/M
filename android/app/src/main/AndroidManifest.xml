<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!-- ── شاشةُ السيارة ────────────────────────────────────────────
             فئةُ POI: تطبيقٌ يعرض نقاطَ اهتمامٍ من نوعٍ واحد. وليس في بيانات
             هذه المنصّة نوعُ مكانٍ غيرُ محطة الوقود، فالشرطُ مستوفًى بالبناء
             لا بالانضباط. ولا تُستعمل فئةُ NAVIGATION: مراجعتُها أضيق،
             وتطلب إذنَ ACCESS_SURFACE، وهذا التطبيق لا يرسم خريطةً ولا يحسب
             طريقاً — يسلّم الوجهةَ لخرائط السيارة. -->
        <service
            android:name=".car.MuhtaCarAppService"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.POI" />
            </intent-filter>
        </service>

        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="1" />
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- قوالبُ الخرائط في السيارة — يلزم لـPlaceListMapTemplate. -->
    <uses-permission android:name="androidx.car.app.MAP_TEMPLATES" />

    <!-- ولا `android.hardware.type.automotive` هنا.
         رفضت Play الحزمةَ صراحةً: «The app cannot declare
         'android.hardware.type.automotive' device feature and
         'com.google.android.gms.car.application' metadata at the same time».
         والاثنان نظامان مختلفان لا وجهان لواحد: تلك الميزةُ تعني **Android
         Automotive OS** — نظامُ سيّارةٍ مدمَجٌ يعمل التطبيقُ داخله؛ وهذه
         البياناتُ تعني **Android Auto** — عرضٌ من الهاتف على شاشة السيارة.
         والمطلوبُ الثاني، فتُحذف الأولى. -->
</manifest>
